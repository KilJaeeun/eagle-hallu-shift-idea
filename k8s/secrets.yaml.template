# Template for K8s secrets
# Copy this to secrets.yaml and fill in the actual values
# DO NOT commit secrets.yaml to git!

apiVersion: v1
kind: Secret
metadata:
  name: eagle-hallushift-secrets
type: Opaque
stringData:
  hf-token: "YOUR_HF_TOKEN_HERE"
  wandb-api-key: "YOUR_WANDB_KEY_HERE"
  github-token: "YOUR_GITHUB_TOKEN_HERE"
